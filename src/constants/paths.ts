enum Paths {
  HOME = "/home",
  JOB_INFO = "/job-info",
  ORGAN_INFO = "/organ-info",
  COMMUNITY = "/community",
  QNA = "/qna",
  LOGIN = "/auth/sign-in",
  REGISTER = "/auth/sign-up",
  CHAT = "/my-page/chat",
  POSTS = "/my-page/posts",
  SCRAPS = "/my-page/scraps",
  USER = "/my-page/user",
}

export default Paths;
