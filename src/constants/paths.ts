enum Paths {
  HOME = "/home",
  JOB_INFO = "/job-info",
  ORGAN_INFO = "/organ-info",
  COMMUNITY = "/community",
  QNA = "/qna",
  LOGIN = "/sign-in",
  REGISTER = "/sign-up",
}

export default Paths;
